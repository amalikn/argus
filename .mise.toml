# Runtime pins for the Argus fork.
# node: vite 7 requires ^20.19.0 || >=22.12.0 and upstream @types/node is ^20.x, so 22 is the lowest version satisfying both.
# python: repo tooling scripts (fixture scan, markdown formatting) run on the workspace default.
[tools]
node = "22"
python = "3.14"
